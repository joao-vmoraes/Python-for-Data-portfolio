SELECT * FROM TABLENAME

INSERT INTO TABLENAME (PAR.)
VALUES
(aaa,bbb,ccc),
(xxx,yyy,zzz)

DELETE FROM TABLENAME
WHERE ...

UPDATE TABLENAME 
SET PAR. = 'aaa'
WHERE xxx = zzz

ORDER BY
ASC
DESC
INNER JOIN
LEFT JOIN
RIGHT JOIN
BETWEEN
GROUP BY
COUNT
SUM
